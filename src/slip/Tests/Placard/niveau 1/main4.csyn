main()
{
  x = 2 ;
  while (x != 0)
    x = (x - 1) ;
  while (x == 0)
    x = (x - 1) ;
  while (x < 0)
    x = (x + 3) ;
  while (x > 0)
    x = (x - 4) ;
  while (x <= 0)
    x = (x + 5) ;
  while (x >= 0)
    x = (x - 6) ;
  return (5) ;
}
